import LearningModal from './LearningModal.vue';

export default {
  title: 'Components/LearningModal',
  component: LearningModal,
  tags: ['autodocs'],
};

export const Default = { args: {
  isOpen: true
} };
export const Empty = { args: {
  isOpen: false
} };
export const Alternative = { args: {
  isOpen: true
} };
